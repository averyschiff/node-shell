const fs = require('fs');

function localLS(fileSystem){
  fs.readdir(fileSystem, "utf8", (err, files) => {
    if (err) {
      done("Something went wrong!");
    } else {
      files.forEach
      if(fileSystem){
        
      }
    }
}
